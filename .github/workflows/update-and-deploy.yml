name: 📊 Update Stock Data & Deploy Pages

on:
  # 每週一到週五的台灣時間 16:00 (市場收盤後) 執行
  schedule:
    - cron: '0 8 * * 1-5'  # UTC 08:00 = 台北 16:00
  
  # 手動觸發
  workflow_dispatch:
  
  # 推送到 main 分支時觸發
  push:
    branches: [ main ]

# 設定權限
permissions:
  contents: read
  pages: write
  id-token: write

# 確保同時只有一個部署在執行
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # 收集股票資料
  collect-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: 🔧 Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'
    
    - name: 📊 Run stock data collector
      run: |
        echo "🚀 開始收集股票資料..."
        dotnet run
        echo "✅ 資料收集完成"
    
    - name: 📁 Check for data changes
      id: changes
      run: |
        if [[ -n $(git status --porcelain data/) ]]; then
          echo "has_changes=true" >> $GITHUB_OUTPUT
          echo "📈 偵測到新的股票資料"
        else
          echo "has_changes=false" >> $GITHUB_OUTPUT
          echo "ℹ️ 沒有新的資料變化"
        fi
    
    - name: 💾 Commit and push data
      if: steps.changes.outputs.has_changes == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action Bot"
        git add data/
        git commit -m "📊 Auto-update stock data - $(date +'%Y-%m-%d %H:%M') 🤖"
        git push
        echo "✅ 資料已推送至 GitHub"

  # 部署到 GitHub Pages  
  deploy-pages:
    needs: collect-data
    runs-on: ubuntu-latest
    if: always()  # 即使沒有新資料也要部署頁面
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      with:
        ref: main  # 確保使用最新的 main 分支
    
    - name: 🛠️ Setup Pages
      uses: actions/configure-pages@v4
    
    - name: 📦 Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        # 上傳整個專案根目錄 (包含 HTML, CSS, JS, data 資料夾)
        path: '.'
    
    - name: 🚀 Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
    
    - name: 🎉 Deployment success
      run: |
        echo "🌟 GitHub Pages 部署成功!"
        echo "🔗 網站 URL: ${{ steps.deployment.outputs.page_url }}"